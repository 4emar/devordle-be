{
  "version": 2,
  "builds": [
    {
      "src": "server.js",
      "use": "@now/node"
    }
  ],
  "routes": [
    { "src": "/api/randomNumber", "dest": "/server.js" },
    { "src": "/(.*)", "dest": "/" }
  ]
}
